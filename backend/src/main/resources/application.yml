spring:
  application:
    name: finance-ai

  data:
    mongodb:
      uri: ${MONGODB_URI}

server:
  port: 8080

jwt:
  secret:  ${JWT_SECRET}
  expiration: 86400000

springdoc:
  swagger-ui:
    path: /swagger-ui.html

gemini:
  api:
    key: ${GEMINI_API_KEY}