services:
  - type: web
    name: finance-ai-backend

    env: docker

    rootDir: backend

    dockerfilePath: ./Dockerfile

    plan: free

    envVars:
      - key: MONGODB_URI
        sync: false

      - key: JWT_SECRET
        sync: false

      - key: GEMINI_API_KEY
        sync: false